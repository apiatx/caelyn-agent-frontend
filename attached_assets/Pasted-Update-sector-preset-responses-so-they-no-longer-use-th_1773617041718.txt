Update sector preset responses so they no longer use the old "Top Moves" section and instead return the exact stock-watch structure I want.

You only have access to the BACKEND repo.

Context:
- Sector preset responses are working again.
- However, the output still shows the old section label/structure like "Top Moves".
- I want the backend response contract and output shape changed so sector preset responses return a new stock-watch section instead.

Important:
- Do NOT focus on frontend changes.
- Do NOT redesign unrelated response sections.
- Keep this as a targeted backend change to the sector preset response contract / prompt / formatter / parser path.

## What I want instead of "Top Moves"

Replace the old "Top Moves" / "Top Moves Today" section for sector preset responses with:

### Watchlist Today
This section must include:
- 1–3 large caps
- 1–3 mid-cap growth names
- 1–3 low caps
- #1 buy right now

These names must come from the selected sector only.

## How the names should be selected
Use all the data already being pulled by the backend and prioritize names showing some combination of:
- relative strength
- current/upcoming catalysts
- hype / social momentum
- institutional flows if available
- insider buying if available
- news momentum
- fundamental strength
- technical setup quality

Important:
- This should not be a random list
- This should be an intentionally ranked sector watchlist
- "mid-cap growth" should not just mean generic mid-cap; it should favor names with stronger growth / momentum / expansion characteristics
- "low caps" means low-cap / small-cap / micro-cap depending on what data exists in that sector universe
- "#1 buy right now" should be the single highest-conviction current setup from the selected sector based on the available evidence

## Required backend changes

1. Audit where the old "Top Moves" label and structure are coming from.
2. Replace that section in the sector preset response contract/prompt/output schema.
3. Ensure the model is explicitly instructed to output:
   - watchlist_today.large_caps
   - watchlist_today.mid_cap_growth
   - watchlist_today.low_caps
   - watchlist_today.number_one_buy_right_now
4. Make the output human-friendly and sector-specific.
5. Keep the rest of the sector response structure intact if it is already useful.
6. If needed, add backward-compatible fallback handling so the response does not go blank if one bucket has thin data.
7. Make sure the selected names come from the chosen sector universe, not broad market names outside the sector.
8. Make sure the section label is no longer "Top Moves" for sector preset responses.

## Desired response shape

For sector preset responses, return something like:

{
  "display_type": "sector_intel",
  "human_label": "AI / Compute",
  "watchlist_today": {
    "large_caps": [
      {
        "ticker": "NVDA",
        "company": "NVIDIA",
        "why_now": "...",
        "key_drivers": ["..."],
        "conviction": "High"
      }
    ],
    "mid_cap_growth": [
      {
        "ticker": "MRVL",
        "company": "Marvell",
        "why_now": "...",
        "key_drivers": ["..."],
        "conviction": "High"
      }
    ],
    "low_caps": [
      {
        "ticker": "CRDO",
        "company": "Credo",
        "why_now": "...",
        "key_drivers": ["..."],
        "conviction": "Medium"
      }
    ],
    "number_one_buy_right_now": {
      "ticker": "NVDA",
      "company": "NVIDIA",
      "why_now": "...",
      "key_drivers": ["..."],
      "conviction": "High"
    }
  }
}

This exact JSON shape can be adjusted to fit the current architecture, but the section concept and labels need to match this intent.

## Important implementation rules

- Keep the fix targeted and minimal.
- Do not break working sector responses again.
- Do not require frontend changes just to restore output.
- If existing parser/formatter expects old keys, make the backend tolerant/backward-compatible where possible.
- Preserve sector-specific ETF/benchmark rows and other useful sector sections.

## Deliverables

1. Implement the backend change.
2. Show exact files changed.
3. Explain where the old "Top Moves" section was still coming from.
4. Explain how "Watchlist Today" is now generated.
5. Explain how the 4 buckets are selected:
   - large caps
   - mid-cap growth
   - low caps
   - #1 buy right now
6. Confirm that the section label "Top Moves" is no longer used for sector preset responses.
7. Test at least:
   - AI / Compute
   - Energy
   - Real Estate

## Acceptance criteria

The task is done when:
- sector preset responses no longer show "Top Moves"
- they instead show "Watchlist Today"
- the response includes:
  - 1–3 large caps
  - 1–3 mid-cap growth
  - 1–3 low caps
  - #1 buy right now
- selections are based on the actual available sector data already being pulled
- sector preset responses remain non-blank and working
- non-sector prompts are not broken

Start by tracing exactly where "Top Moves" is still being injected into the sector preset output path, then replace it with the new watchlist structure in the smallest safe way.