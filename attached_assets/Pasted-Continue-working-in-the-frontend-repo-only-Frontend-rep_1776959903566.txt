Continue working in the frontend repo only:

* Frontend repo: https://github.com/apiatx/caelyn-agent-frontend.git

Workflow rules:

* Work directly on `main`
* Do NOT create a branch
* Do NOT create a PR
* Commit directly to `main`
* Push directly to `main`

This is a focused Home-page cleanup pass. Do not broaden scope.

PRIMARY GOALS

1. Remove the practical duplication between:

* `Highlighted Companies`
* `Watchlist Snapshot`

As shown in the current Home page, these are basically duplicates:

* same watchlist-driven names
* similar ordering/signal story
* Highlighted Companies mainly adds signal comments like:

  * vol spike
  * momentum
  * hot
    etc.

I want to consolidate that useful context into the Watchlist Snapshot section and stop wasting a separate section on duplicate information.

Frontend goal:

* treat `watchlist_snapshot` as the main watchlist section
* surface the signal labels/context next to ticker names or in the signal column
* either remove the Highlighted Companies section from Home entirely OR replace it with something truly different only if backend still requires temporary compatibility
* strong preference: remove it from the Home layout if Watchlist Snapshot now contains the important signal context

2. Trending on X and Trending on Stocktwits are showing too few tickers
   Audit whether the frontend is limiting them after the recent layout move.

I need:

* Trending on X to show all the tickers returned by backend for Home
* Trending on Stocktwits to show all the top trending items returned by backend for Home
* do not artificially slice/cap the arrays unless there is a very strong visual reason
* if there must be a cap in the main card, it should be a generous one and not a silent regression

3. Trending on X popup/modal/chart window should show full blurb text
   Right now the main Home card text is cut off, which is fine.
   But when the user clicks the ticker and opens the popup TradingView chart/modal, the info blurb/rationale for that X ticker should be visible in full there.

AUDIT FIRST
Inspect and report:

* how Highlighted Companies and Watchlist Snapshot are currently wired
* whether frontend is duplicating the same dataset in two different sections
* whether frontend currently slices or limits:

  * `trending_on_x.top_tickers`
  * `trending_ideas`
* where the X ticker popup/modal gets its text content from
* whether full rationale exists already in payload but is being visually clipped, or whether the frontend only stores/renderers the truncated version

IMPLEMENT
A. Watchlist consolidation

* if backend has enriched `watchlist_snapshot`, use it as the main source
* move signal labels such as “vol spike”, “momentum”, “hot” into the Watchlist Snapshot UI
* remove Highlighted Companies from the Home page layout if it is now redundant
* if you keep it temporarily for compatibility, it must not feel like a duplicate block

B. Trending counts

* remove frontend-only slices/caps that are causing fewer X/Stocktwits items to show than before
* render all backend-returned items, or a clearly intentional generous limit if truly necessary
* do not add new fetches

C. X popup full text

* ensure the ticker click / popup / chart modal renders the full rationale/blurb text for Trending on X items
* it is okay for the Home card preview to stay truncated
* but the popup must show the complete text body cleanly

D. Layout cleanliness

* after removing/consolidating Highlighted Companies, make the layout still feel balanced
* do not redesign the whole page
* just reclaim the wasted space intelligently

VERIFY
Before finishing, verify:

* Watchlist Snapshot now carries the useful signal context
* Highlighted Companies is removed or no longer redundant
* Trending on X shows all intended backend items
* Trending on Stocktwits shows all intended backend items
* clicking a Trending on X ticker opens the popup/modal with full rationale text visible
* no new page-level fetches were added
* query count did not increase

DELIVERABLES
When done, report:

1. exact frontend files changed
2. whether Highlighted Companies was removed or repurposed
3. how signal labels/context are now shown in Watchlist Snapshot
4. exact cause of the reduced visible ticker counts for:

   * Trending on X
   * Trending on Stocktwits
5. whether frontend truncation/slicing was fixed
6. how the full X rationale text is now shown in the popup/modal
7. whether query count changed
8. confirmation changes were committed and pushed directly to `main`

EXECUTION STYLE

* Audit first
* Consolidate duplicate information
* Fix the silent count regression
* Fix popup detail rendering properly
* Keep the page cleaner and more useful
* Then commit and push directly to `main`
