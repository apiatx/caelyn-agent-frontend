You are working in this repo only:

* Frontend repo: https://github.com/apiatx/caelyn-agent-frontend.git

Workflow rules:

* Work directly on `main`
* Do NOT create a branch
* Do NOT create a PR
* Commit directly to `main`
* Push directly to `main`

This is a focused Home page UI feature pass. Do not broaden scope.

PRIMARY GOAL
Add one shared toggle control for the Home page `Top Gainers` and `Top Losers` sections with these 5 options:

* All
* Stocks
* Commodities
* Crypto
* ETFs

This is one toggle that controls both sections at the same time.

REQUIREMENTS

1. The design/layout of both sections must remain exactly the same no matter which category is selected
2. Stocks should look exactly like the current version since that behavior is already correct
3. Both sections should update together when the shared category toggle changes
4. Do not redesign the cards unnecessarily
5. Keep the UI premium and consistent with the rest of the Home page

AUDIT FIRST
Inspect the current Home page implementation and determine:

* where the Top Gainers / Top Losers sections are rendered
* what data source currently powers them
* what existing segmented control / pill toggle / filter UI components already exist and can be reused
* how to add one shared control cleanly without messing up layout

IMPLEMENT
A. Add one shared category toggle

* options:

  * All
  * Stocks
  * Commodities
  * Crypto
  * ETFs
* place it cleanly in the Home page near the Top Gainers / Top Losers sections
* one toggle controls both sections

B. Wire both sections to the selected category

* use the backend-normalized category movers payload
* when the toggle changes, both sections should switch together
* do not create five completely separate ugly render paths
* same row/card layout for every category

C. Preserve design consistency

* keep the exact same structural design for both sections regardless of selected category
* do not make crypto look like one component and commodities look like another
* same cards, same spacing, same typography, same row rhythm

D. State handling

* graceful loading state when switching categories
* graceful empty/error state if one category has no data
* do not regress current Stocks experience

E. “All” behavior

* render the backend-defined “All” result cleanly with no layout differences
* preserve source/category info only if it fits naturally in the existing design

VERIFY
Before finishing, verify:

1. one toggle controls both sections together
2. Stocks still looks/behaves exactly as before
3. All / Stocks / Commodities / Crypto / ETFs all render in the same UI design
4. layout remains clean
5. query count does not become stupid
6. no unrelated Home sections are broken

DELIVERABLES
When done, report:

1. exact frontend files changed
2. where the shared toggle was added
3. how the category state is wired to both sections
4. confirmation that Stocks behavior/design was preserved
5. confirmation all 5 categories use the same visual design
6. whether query count changed
7. confirmation changes were committed and pushed directly to `main`

EXECUTION STYLE

* Audit first
* Keep the feature simple and clean
* One toggle, two sections, one consistent design
* Then commit and push directly to `main`
