Make a minimal, targeted fix to sector preset responses in CaelynAI.

You only have access to the BACKEND repo.

Important constraints:
- Do NOT redesign the sector feature.
- Do NOT refactor unrelated code.
- Do NOT change any non-sector preset behavior.
- Do NOT change the overall sector response structure beyond the exact 3 requested fixes below.
- The fix must work for:
  - Caelyn / agent collaboration mode
  - every solo LLM mode
  - any model combination
- All model paths must return the same structured sector response shape for sector preset buttons.

Context:
The sector preset buttons on the terminal page are:
- Energy
- Materials
- Aerospace/Defense
- Tech
- AI/Compute
- Quantum
- Fintech
- Biotech
- Real Estate

A sector preset response currently contains sections like:
- What's Moving
- Top Signal From Each Scanner
- Top Moves Today
- Upcoming Catalysts
- Portfolio Bias

I want ONLY these exact changes:

## Change 1 — Replace the current "Top Moves Today" section behavior
For sector preset responses only:
- stop returning the current "Top Moves Today" content/label
- replace it with a section labeled exactly: "Watchlist Today"

This new "Watchlist Today" section must return:
- 1–3 large caps
- 1–3 mid-cap growth names
- 1–3 low caps
- 1 single "#1 Buy Right Now"

Selection must be based on the data already being pulled in the backend, using the same available evidence such as:
- social momentum
- earnings
- fundamentals / financials
- analyst sentiment if available
- technical setups
- news / catalysts

Requirements:
- stay inside the selected sector universe
- do not return a generic broad-market list
- rank intentionally, not randomly
- if a bucket has thin data, still return the best available names without breaking the response
- preserve the rest of the sector response structure

## Change 2 — Fix "What's Moving"
Right now, each row under "What's Moving" is showing the sector name (example: "AI/Compute") instead of a ticker.

Fix this so that for sector preset responses:
- each "What's Moving" row shows a ticker
- not the sector preset name
- this must work for every sector preset, not just AI/Compute

If a row is tied to a company-specific move, the displayed label should be the relevant ticker.
Do not leave the sector name repeated on every row.

## Change 3 — Rename and repurpose "Strongest Sector"
Inside "Top Signal From Each Scanner", the current signal:
- "Strongest Sector"

must be changed to:
- "Strongest Overall"

And its meaning should be:
- the best overall investment / trade / fundamental leader with active catalysts at the time of the prompt

Requirements:
- keep it in the same general scanner-signal area
- keep the same overall structured response style
- only change this one signal name and its ranking intent
- do not change the other scanner labels unless necessary for this exact fix

## Critical consistency requirement
These exact sector response changes must apply consistently across:
- Caelyn / all-agent collaboration mode
- any solo LLM mode
- any mixed model flow

That means:
- sector preset buttons must resolve into the same structured response contract regardless of which model is answering
- no model-specific fallback should return the old "Top Moves Today" shape

## Implementation guidance
Audit the exact sector preset response pipeline and make the minimum necessary changes in the real response contract / prompt / formatter / parser path.

Specifically verify:
1. where "Top Moves Today" is currently coming from
2. where "What's Moving" row labels are populated
3. where "Strongest Sector" is defined
4. how the sector response structure is enforced across different LLM modes

If there are multiple contracts/prompts/parsers for different model paths, unify only the sector response shape for these exact three items.

## Deliverables
1. Implement only these 3 changes.
2. Show exact files changed.
3. Explain exactly where each of the three old behaviors was coming from:
   - "Top Moves Today"
   - repeated sector name under "What's Moving"
   - "Strongest Sector"
4. Explain how you made the sector response shape consistent across all model modes.
5. Confirm you did NOT change unrelated response sections or non-sector prompts.

## Acceptance criteria
The task is done when:
- sector preset responses show "Watchlist Today" instead of "Top Moves Today"
- "Watchlist Today" includes:
  - 1–3 large caps
  - 1–3 mid-cap growth
  - 1–3 low caps
  - #1 Buy Right Now
- "What's Moving" shows tickers instead of the sector name
- "Strongest Sector" is renamed to "Strongest Overall"
- "Strongest Overall" represents the best overall investment/trade/fundamental leader with catalysts at the time
- the same structured sector response appears in Caelyn mode and all solo LLM modes
- nothing else is changed

Do not get creative. Make the smallest safe fix that changes only these exact sector response behaviors.