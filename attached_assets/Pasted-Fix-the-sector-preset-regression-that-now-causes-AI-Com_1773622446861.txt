Fix the sector preset regression that now causes AI/Compute (and potentially other sector buttons) to return:

"The AI took too long to respond. Please try again."

You only have access to the BACKEND repo.

Important constraints:
- Do NOT redesign the sector feature.
- Do NOT change unrelated prompts or non-sector flows.
- Do NOT make broad refactors.
- The goal is to restore fast, working sector responses while preserving the three requested sector changes:
  1) rename the old section output to "Watchlist Today"
  2) make "What's Moving" use tickers instead of the sector name
  3) rename "Strongest Sector" to "Strongest Overall"
- Keep the fix minimal and safe.

## Current problem
After the recent sector changes, clicking a sector preset like AI/Compute now returns:
"The AI took too long to respond. Please try again."

This strongly suggests the latest backend changes introduced one of these regressions:
- prompt became too large / too strict and is causing long generation time
- parser/contract mismatch is causing retries/fallbacks until timeout
- new watchlist logic is too expensive or hangs on ranking/bucketing
- one model path is waiting on missing fields / malformed structured output
- sector response generation is now slower or broken in one or more model modes

## Your task
Audit the sector preset request path end-to-end and identify the exact cause of the timeout.

Specifically inspect:
1. preset routing for sector buttons
2. data gathering for sector/thematic requests
3. prompt construction after the latest sector contract changes
4. model response parsing / validation / retry logic
5. any newly introduced watchlist bucketing logic
6. any code path differences across:
   - solo model mode
   - Caelyn / collaboration mode
   - different LLMs

## Strong suspicion areas to verify
Check whether:
- the new sector prompt contract is too verbose and causing timeout
- structured output requirements are too strict and the model fails/retries repeatedly
- "Watchlist Today" bucketing logic is being done inside the model prompt in a way that bloats the response or breaks parsing
- "What's Moving" ticker extraction is causing bad field expectations
- "Strongest Overall" renamed field is mismatched with parser expectations
- one fallback path still expects the old sector response shape and hangs
- the timeout is caused by parser/formatter retries, not raw model latency

## Required outcome
Restore working sector preset responses first.

Priority order:
1. sector buttons return a response again
2. response is fast enough to complete normally
3. the three requested sector changes still work
4. behavior is consistent across solo modes and Caelyn mode

## Implementation rules
- Make the smallest safe fix.
- If necessary, simplify the new sector response contract so it reliably completes.
- If the new "Watchlist Today" structure is causing timeout, keep the label and requested semantics but reduce prompt complexity and/or make parser handling more tolerant.
- Preserve backward compatibility where possible so no frontend changes are needed just to get responses working again.
- Do not change anything outside sector preset response handling unless absolutely required.

## Deliverables
1. Identify the exact root cause of the timeout.
2. Implement the minimum fix.
3. Show exact files changed.
4. Explain why the last sector changes caused timeout / blank response behavior.
5. Confirm the fix was tested with at least:
   - AI/Compute
   - Energy
   - Real Estate
6. Confirm it works in:
   - at least one solo model mode
   - Caelyn / collaboration mode

## Acceptance criteria
The task is done when:
- clicking AI/Compute returns a normal sector response again
- sector responses no longer timeout
- "What's Moving" shows tickers instead of repeated sector names
- "Strongest Sector" is replaced by "Strongest Overall"
- the watchlist section works again
- no unrelated prompt types are broken

Start by tracing one real failing AI/Compute sector request through routing, prompt build, model call, parsing, and final response serialization, then fix only the specific cause of the timeout.