FRONTEND — USE BACKEND FULL-UNIVERSE CAELYN CONFLUENCE FIELDS AND FIX EMPTY CATALYST/THEME/RISK TABS

Frontend only.

Do not change backend scoring.
Do not change backend endpoints unless you prove no existing endpoint returns the required fields.
Do not call LLMs.

Backend now returns full-universe Confluence ranking fields:

caelyn_confluence_score
caelyn_confluence_bucket
caelyn_confluence_reason_codes
confluence_at_support
confluence_at_support_score
confluence_at_support_state
confluence_at_support_reason_codes
entry_risk_reward_state
entry_risk_reward_score
active_support_status
lower_low_confirmed
catalyst_alignment_score
catalyst_primary_source
catalyst_primary_event
catalyst_rss_event
catalyst_scheduled_event
catalyst_bearish_conflict
theme_policy_available
theme_policy_boost
theme_policy_event
theme_policy_theme
investment_alignment_score
investment_alignment_available
investment_alignment.unavailable_reason

Goal:

The Caelyn Confluence UI must show all watchlist/confluence rows ranked by caelyn_confluence_score, and tabs must be filters over that full row set.

Current UI bugs:

1. Actionable Setups and Investment Quality show “Catalyst: none” everywhere.
2. Theme Policy Tailwinds tab is empty.
3. New Catalysts tab is empty.
4. Risk / Conflicts tab is empty.
5. It is unclear whether the UI is using all 379 scored rows or a partial/stale endpoint.
6. Overextended names must not appear as Actionable if backend bucket is WATCH_FOR_RESET.

==================================================
PART 0 — DATA SOURCE AUDIT
==================================================

Before editing, inspect what caelyn-confluence.tsx actually receives.

Log/print:

endpoint used
watchlist id used
row count received
unique symbols received
sample row keys for MRVL, GLW, BE, CRWD, FTNT, MARA if present

Return counts:

rows with caelyn_confluence_score
rows with caelyn_confluence_bucket
rows with catalyst_alignment_score > 0
rows with catalyst_primary_event
rows with catalyst_rss_event
rows with catalyst_scheduled_event
rows with theme_policy_boost > 0
rows with theme_policy_event
rows with catalyst_bearish_conflict
rows with confluence_at_support_state
rows with entry_risk_reward_state
rows with WATCH_FOR_RESET bucket
rows with RISK_CONFLICT bucket

Expected:

Frontend should receive close to the backend full universe, e.g. 379 rows, not 50, not 7, not only visible table rows.

If the current endpoint returns only 50 rows, stop using it for Caelyn Confluence.

Prefer the full Watchlist Alignment / Confluence endpoint that returns the 379-row set with the new fields.

==================================================
PART 1 — ALL CONFLUENCE TAB
==================================================

Add or make default an “All Confluence” tab.

This should show every row with caelyn_confluence_score.

Sort by:

caelyn_confluence_score descending

Display compact columns/cards:

rank
ticker
caelyn_confluence_score
caelyn_confluence_bucket
actionability
trade_alignment_score
investment_alignment_score
entry_risk_reward_state
confluence_at_support_state
catalyst_alignment_score
theme_policy_boost
risk/conflict indicator

This is the main page. Do not hide most names.

Show coverage:

Confluence coverage: [rows received] / [backend expected or watchlist count]

==================================================
PART 2 — ACTIONABLE SETUPS TAB
==================================================

Use backend bucket, not old frontend guessing.

Include only:

caelyn_confluence_bucket in:
ACTIONABLE
NEAR_ACTIONABLE
CONFLUENCE_AT_SUPPORT

Exclude:

WATCH_FOR_RESET
RISK_CONFLICT
NO_CLEAR_CONFLUENCE

Also exclude if:

actionability is TOO_EXTENDED / AVOID
entry_risk_reward_state is STRONG_ASSET_EXTENDED_WAIT / BROKEN_SUPPORT_AVOID
extension_state is EXTREME_EXTENSION / VERTICAL / CROWDED_MOVE
lower_low_confirmed is true

Sort by:

caelyn_confluence_score descending

Validate:

CRWD / FTNT / AMD / MU / MRVL should not appear in Actionable Setups if bucket is WATCH_FOR_RESET.

==================================================
PART 3 — INVESTMENT QUALITY TAB
==================================================

Investment Quality is separate from Actionable Setups.

Rank by:

investment_alignment_score descending

Show:

investment_alignment_score
caelyn_confluence_score
caelyn_confluence_bucket
actionability
entry_risk_reward_state
catalyst_alignment_score
investment unavailable reason if missing

It is okay for extended names to appear here, but they must be clearly labeled:

Strong Investment, Entry Extended
Strong Investment, Watch for Reset
Strong Investment, Broken/Risk
Strong Investment, Near Support

Do not imply actionable just because Investment is strong.

==================================================
PART 4 — THEME POLICY TAILWINDS TAB
==================================================

Use the full row set.

Detect:

theme_policy_boost > 0
or theme_policy_available === true
or theme_policy_event exists

Group by theme_policy_theme.

Show:

theme
policy event title
policy score
boost
affected tickers
affected ticker count

If empty, show debug counts from Part 0. Do not silently show empty if fields exist.

==================================================
PART 5 — NEW CATALYSTS TAB
==================================================

Use the full row set.

Detect any of:

catalyst_primary_event
catalyst_rss_event
catalyst_scheduled_event
catalyst_v2_primary_event
catalyst?.primary_event
catalyst?.rss_event
catalyst?.scheduled_event
catalyst?.v2_primary_event

Also display rows with catalyst_alignment_score > 0 even if the event object is missing, but label event as “event details unavailable” so we can catch response-shape gaps.

Show:

ticker
catalyst score
source
event title if present
event type if present
theme policy boost if present
bearish conflict if present

Do not show “Catalyst: none” when catalyst_alignment_score > 0.

==================================================
PART 6 — RISK / CONFLICTS TAB
==================================================

Use backend bucket first.

Include rows where:

caelyn_confluence_bucket = RISK_CONFLICT
or catalyst_bearish_conflict exists
or lower_low_confirmed is true
or entry_risk_reward_state = BROKEN_SUPPORT_AVOID
or active_support_status = lost_confirmed
or actionability = AVOID
or actionability = TOO_EXTENDED
or caelyn_confluence_bucket = WATCH_FOR_RESET

Sort by:

risk severity
then caelyn_confluence_score descending

Risk severity:

RISK_CONFLICT bucket +100
lower_low_confirmed +95
BROKEN_SUPPORT_AVOID +90
AVOID +90
WATCH_FOR_RESET +75
STRONG_ASSET_EXTENDED_WAIT +75
bearish conflict +70
TOO_EXTENDED +70

Show why the row is in risk.

Validate:

CRWD / FTNT / AMD / MU / MRVL should appear as Watch for Reset / extended risk, not Actionable.

PLTR / OKLO / SOFI should appear as Risk if backend bucket says RISK_CONFLICT.

==================================================
PART 7 — FIX CATALYST DISPLAY IN CARDS
==================================================

Every card should display:

If catalyst_alignment_score > 0:
Catalyst: [score]

If catalyst event object exists:
show event title/source

If no event object but score > 0:
show:
Catalyst: [score] — details unavailable

If catalyst_alignment_score is null/0:
Catalyst: None

Do not show Catalyst None for rows with catalyst_alignment_score > 0.

==================================================
PART 8 — VALIDATION SYMBOLS
==================================================

Validate:

GLW
VRT
AME
TSM
BE
NVEC
MARA
CRWD
FTNT
AMD
MU
MRVL
PLTR
OKLO
SOFI
NVDA

For each, return:

present yes/no
caelyn_confluence_score
caelyn_confluence_bucket
actionability
entry_risk_reward_state
investment_alignment_score
catalyst_alignment_score
theme_policy_boost
which tabs it appears in

Expected:

GLW: top All Confluence / Actionable if backend says ACTIONABLE
BE: support/confluence or near-actionable, not support lost
MARA: near-actionable/support setup if backend says so
CRWD/FTNT: Watch for Reset / Risk, not Actionable
AMD/MU/MRVL: Watch for Reset if backend says so
PLTR/OKLO/SOFI: Risk if backend says RISK_CONFLICT
NVDA: Investment Quality if high IA but stale entry

==================================================
FINAL RESPONSE
==================================================

Return:

FRONTEND FULL-UNIVERSE CONFLUENCE TAB REPAIR REPORT

FILES CHANGED:
[list]

DATA SOURCE BEFORE:
[endpoint + row count]

DATA SOURCE AFTER:
[endpoint + row count]

COVERAGE:
[rows received / expected]

ALL CONFLUENCE TAB:
[summary]

ACTIONABLE SETUPS:
[summary + excluded extended names]

INVESTMENT QUALITY:
[summary]

THEME POLICY TAILWINDS:
[summary + counts]

NEW CATALYSTS:
[summary + counts]

RISK / CONFLICTS:
[summary + counts]

CATALYST DISPLAY FIX:
[summary]

VALIDATED SYMBOLS:
[table]

BACKEND CHANGES:
[NO expected]

End exactly:

FRONTEND FULL-UNIVERSE CONFLUENCE VERDICT

USES FULL BACKEND CONFLUENCE ROW SET:
[YES/NO]

ALL CONFLUENCE TAB SHOWS ALL SCORED ROWS:
[YES/NO]

ACTIONABLE SETUPS EXCLUDES WATCH_FOR_RESET / RISK:
[YES/NO]

INVESTMENT QUALITY SEPARATE FROM ACTIONABLE:
[YES/NO]

THEME POLICY TAILWINDS WORKING:
[YES/NO]

NEW CATALYSTS WORKING:
[YES/NO]

RISK / CONFLICTS WORKING:
[YES/NO]

CATALYST SCORE NO LONGER DISPLAYS NONE WHEN SCORE EXISTS:
[YES/NO]

VALIDATION SYMBOLS ACCOUNTED FOR:
[YES/NO]

BACKEND UNCHANGED:
[YES/NO]

READY FOR USER REVIEW:
[YES/NO]