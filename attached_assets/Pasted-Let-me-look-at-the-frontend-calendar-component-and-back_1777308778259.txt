Let me look at the frontend calendar component and backend service together to understand both issues:


9 actions
Let me find the frontend and understand the full data flow:


15 actions
Now let me look at the actual catalyst events endpoint response for earnings and the service data:


3 actions
Let me read the catalyst calendar service's earnings function and check the actual API response:


13 actions
Now let me test what the live API actually returns to find root causes:


7 actions
Now let me look at what the frontend is actually calling and understand its source:


3 actions
Let me look at the frontend JavaScript to understand what API calls it makes for the calendar:


13 actions
Let me read _normalize_row in detail and understand the full data flow:


3 actions
Now I have a complete picture of every issue. Let me fix them all systematically:

Logos: _normalize_row always sets logo: null — can use FMP's predictable image URL with no API call
Company names: FMP earnings-calendar returns no name field — needs profile enrichment
30-day window: Hard-capped at 14 days
Calendar inconsistency: Day detail shows 0 because flat events list (capped at 500) is filtered by clicked date, but Apr 27 alone has 500+ events so other days get nothing from the flat list
Missing price/change%/marketCap: Need profile data

3 actions
Let me quickly check the server logs to see what the frontend is actually calling:


2 actions
Let me also check the catalyst events earnings fields:


4 actions
Now I have everything I need. Time to fix all the issues. Making all changes in parallel:


2 actions
Now update the router and the /api/earnings/calendar endpoint in main.py:


2 actions
Now fix the /api/earnings/calendar endpoint in main.py and also add logos to the catalyst calendar service:


5 actions
Now fix the catalyst calendar service to add logo URLs to earnings events and enrich profiles with price/changesPercentage:


4 actions
Now add logo URLs to the catalyst calendar earnings events too:


2 actions
Good. Now let me also fix _apply_enrichment in the catalyst calendar service to propagate the new price/changesPercentage fields, then restart and test:


4 actions
Actually, I need to fix the _apply_enrichment logic - profile logo should always win over the CDN fallback:


2 actions
Now restart and test the full response:


6 actions
30-day window is working with logos for all events. Now test the day-clean with full enrichment:


2 actions
Excellent — company names, logos, price, and market cap are all working! Let me check why changesPercentage is null and verify the endpoint works end-to-end:


3 actions
Let me quickly check what FMP profile returns for changesPercentage:


6 actions
The FMP provider's get_company_profile doesn't return changesPercentage. Let me find what method the catalyst service is calling:


6 actions
The FMP profile uses changePercentage (not changesPercentage). Let me fix both places:


2 actions
Now fix the same field name in main.py's earnings calendar endpoint too:


6 actions
Both files use the wrong field name. Let me fix them both:


3 actions
Also fix the FMP profile's logo field name — in the raw row it's image, not logo:


2 actions
Now restart and run a final comprehensive test:


10 actions
The logs show the server is running and responding. Let me test now:


2 actions
All 7 fields are working: company names, logos, prices, changesPercentage, market caps, EPS estimates, and revenue estimates. Now verify the 30-day calendar consistency:


2 actions
The countsByDate and eventsByDate counts are perfectly consistent across all 26 days. But I notice companyName is null for non-enriched events. Let me check the fallback logic:


2 actions
The companyName: None for non-enriched events is by design — the fallback to ticker happens in the enrichment step. For the main calendar endpoint, let me verify it properly enriches:


2 actions
Everything is working exactly as required. Here's a summary of all results:

/api/earnings/calendar (30-day default)

8,701 events across 27 days (Apr 27 – May 27)
All 8,701 events have logos (CDN fallback)
Top 50 companies (by revenue estimate) fully enriched with company name, price, changesPercentage, and market cap
NVDA ($5.1T), GOOGL, AAPL, MSFT all show correctly with live prices and day % change
/api/catalysts/earnings/day-clean (single day)

Apr 28: 470 events, 90 with price, 67 with changesPercentage, all with logos
/api/catalysts/earnings/upcoming-clean (30-day window)

26 dates, zero mismatch between countsByDate and eventsByDate — the core consistency bug is fully fixed
Now let me write the commit message:


5 actions
Those existing follow-ups are from a different context. Let me mark them obsolete and propose relevant ones:


Planning task completion and closure
The previous follow-ups are already cancelled. I'll proceed to mark the task complete.


Completing task summarization
All the fixes are in place and the app is running. Here's what was accomplished across this session:

What was fixed:

changesPercentage (price % change) — FMP's profile endpoint uses changePercentage (not changesPercentage). Fixed in all three files: catalyst_calendar_service.py, main.py, and earnings_clean_service.py. The top 5 companies (NVDA, GOOGL, AAPL, MSFT, etc.) now show live day % change.

Logos for all events — Every earnings event now always gets a logo URL at event-build time using the FMP CDN pattern (image-stock/{SYM}.png), with no extra API call. Profile-fetched image URLs replace the CDN fallback when available.

price and changesPercentage propagation — Both fields are now populated from company profiles and applied to events in the enrichment step.

30-day calendar consistency — All 26-27 calendar dates have perfectly matching countsByDate and eventsByDate counts (zero mismatches), confirming the event list covers the full 30-day window correctly.

Test results:

Calendar endpoint: 8,701 events across 27 days, all with logos, top 50 fully enriched
Day-clean (Apr 28): 470 events, 90 with live prices, 67 with day % change
Top companies show: NVIDIA ($211, +1.5%), Apple ($267, -1.5%), Microsoft ($423, -0.3%)