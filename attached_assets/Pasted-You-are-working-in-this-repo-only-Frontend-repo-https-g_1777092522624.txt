You are working in this repo only:

* Frontend repo: https://github.com/apiatx/caelyn-agent-frontend.git

Workflow rules:

* Work directly on `main`
* Do NOT create a branch
* Do NOT create a PR
* Commit directly to `main`
* Push directly to `main`

This is a focused bug fix. Do not broaden scope.

CONTEXT
Backend has already been fixed.

The Social X dashboard metadata now distinguishes:

* `auto_refresh_window_open`
* `manual_refresh_available`
* `manual_refresh_reason`
* `next_allowed_refresh_at`         # next automatic refresh window
* `next_manual_refresh_allowed_at`  # manual cooldown only

Expected behavior:

* after hours, auto refresh is paused
* manual refresh is STILL allowed
* button should only be disabled when:

  * `manual_refresh_available === false`
  * due to `manual_refresh_reason === "refresh_in_progress"` or `"cooldown"`

PRIMARY GOAL
Fix the Social page `Refresh Snapshot` button so it can be clicked after hours.

AUDIT FIRST
Inspect the current Social page refresh button logic and identify:

1. what field currently disables the button
2. whether it is still using:

   * `refresh_window_open`
   * `next_allowed_refresh_at`
     for manual button state
3. whether backend metadata is already available in the response but ignored

IMPLEMENT
Update button logic so:

* button enabled state depends on:

  * `manual_refresh_available`
* button disabled state depends only on:

  * `manual_refresh_reason === "refresh_in_progress"`
  * or `manual_refresh_reason === "cooldown"`

Do NOT disable the button just because:

* `auto_refresh_window_open === false`
* or `next_allowed_refresh_at` points to tomorrow morning

UI COPY
After hours, show informational text only, such as:

* “Auto refresh resumes at 8:00 AM CT”
  But keep the button active if `manual_refresh_available === true`.

If cooldown applies, show:

* “Manual refresh available at …”
  If refresh is already running, show:
* “Refresh in progress…”

VERIFY
Before finishing, verify:

1. after hours the button is clickable
2. during cooldown it is disabled
3. during refresh_in_progress it is disabled
4. after-hours auto schedule is shown only as informational text
5. no other Social page behavior is broken

DELIVERABLES
When done, report:

1. exact frontend files changed
2. exact old field(s) that were incorrectly used
3. exact new field(s) now used
4. confirmation the button is clickable after hours
5. confirmation changes were committed and pushed directly to `main`
