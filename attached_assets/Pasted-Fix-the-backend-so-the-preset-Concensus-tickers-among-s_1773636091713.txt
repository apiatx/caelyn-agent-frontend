Fix the backend so the preset:

"Concensus tickers among select X traders"

actually uses the curated 18-account list in the prompt sent to Grok/XAI, instead of returning a generic broader response.

You only have access to the BACKEND repo.

Important constraints:
- Do NOT change the existing broader preset:
  "Consensus tickers among top X traders"
- Do NOT redesign the social page.
- Do NOT make frontend changes.
- Do NOT make broad refactors.
- Keep the fix minimal and targeted.
- The problem is specifically that the new curated-list preset is not actually constraining Grok to the selected accounts.

## Problem
The preset/button:

"Concensus tickers among select X traders"

is NOT returning results based on this exact curated account list:

- @StockSavvyShay
- @HyperTechInvest
- @crux_capital_
- @SJCapitalInvest
- @BlackPantherCap
- @Kaizen_Investor
- @Venu_7_
- @CKCapitalxx
- @TheTape_TNM
- @equitydd
- @Speculator_io
- @DrJebaim
- @StonkValue
- @stamatoudism
- @yianisz
- @sunxliao
- @futurist_lens
- @Thomas_james_1

Instead, it is returning a generic response and not making it clear that these selected accounts are the only accounts being scanned.

These accounts need to be explicitly baked into the Grok prompt/contract/path used by the backend for the curated preset.

## What I need fixed
The backend preset for the curated-list feature must:
- explicitly instruct Grok/XAI to scan only the 18 selected accounts above
- not drift to broader/popular-X consensus behavior
- return output that is clearly based on those specific selected traders
- include those selected accounts in accounts_analyzed / prompt grounding / response grounding

## Your task
Audit the exact backend path for the curated preset:
- x_select_trader_consensus

Trace it end-to-end and find where the selected-account constraint is failing.

Specifically inspect:
1. PRESET_ALIASES / preset resolution
2. INTENT_PROFILES
3. INTENT_TO_CATEGORY
4. _build_prompt injection
5. the exact contract/prompt text sent for x_select_trader_consensus
6. any shared code path that may still be using the broader x_trader_consensus behavior
7. whether the wrong contract is being injected
8. whether the curated account list is being omitted, overwritten, or ignored before the Grok call
9. whether scan_type/title differs but the actual prompt body is still generic

## Strong suspicion areas to verify
Please check whether one of these is happening:
- x_select_trader_consensus routes correctly, but still injects the old/general consensus contract
- the new contract exists, but the actual prompt sent to Grok does not contain the curated account list
- the prompt contains weak wording like "look at these traders" instead of explicitly "search only these 18 selected accounts — no others"
- the broader preset and curated preset share too much code and are effectively sending the same Grok prompt
- accounts_analyzed is only a response schema field, but not actually a hard constraint in the model prompt
- Grok is being asked for consensus, but not forced to anchor to the listed accounts

## Required fix
Make the smallest safe backend fix so that x_select_trader_consensus truly uses the curated 18-account list as a hard prompt constraint.

The Grok prompt/contract for x_select_trader_consensus must explicitly and unambiguously instruct:
- search only these 18 selected accounts
- do not include other broader popular accounts
- synthesize consensus only from these selected accounts
- base all returned tickers/theses on those accounts only

If needed, strengthen the contract wording so the curated account restriction is impossible to miss.

## Desired result
When the curated preset runs, the response should make it obvious it came from these selected accounts:
- through the actual consensus content
- through accounts_analyzed
- through the thesis synthesis
- through the prompt grounding

This preset should not behave like the broad/top-X preset.

## Deliverables
1. Identify the exact root cause of why x_select_trader_consensus is still returning a generic response.
2. Implement the minimum backend fix.
3. Show exact files changed.
4. Show the exact prompt/contract section that now hard-locks the selected 18-account list.
5. Confirm that x_trader_consensus remains unchanged.
6. Confirm that x_select_trader_consensus now uses only the curated 18-account list.

## Acceptance criteria
The task is done when:
- x_select_trader_consensus no longer behaves like the broader top-X preset
- the curated 18-account list is explicitly baked into the Grok prompt sent by the backend
- the response is grounded in those selected traders only
- x_trader_consensus remains unchanged
- no frontend changes are required

Before coding, show me the exact current prompt/contract path for x_select_trader_consensus and tell me whether the selected 18-account list is actually present in the final prompt sent to Grok.