When I upload a CSV transaction file to the portfolio dashboard, there might be options trades on there (buy to open, buy to close, etc) - we need to be able to handle those before I upload my first CSV.

4 minutes ago

Parsing options trade types (5 seconds)
Let me look at the current action/transaction-type detection in the parser so I know exactly what to extend:


2 actions
Now I have a complete picture of the parser. I need to:

Detect options by action keywords (buy to open, sell to close, buy to close, sell to open, expired, assigned, exercised) — before the buy/sell check since "buy to open" contains "buy" and would currently be misread as a stock buy
Detect options by symbol pattern (OCC format, dash prefix, embedded dates, spaces)
Check for an explicit instrument type column saying "Option"
Skip all of them cleanly and report a count back to the frontend

2 actions
Let me check the logs and finish wiring up options_rows to the return value simultaneously:


2 actions
The workflow failed — let me check the return statement and endpoint response while restarting:


3 actions
Syntax is clean — the failure was just the graceful shutdown from my restart. Let me update the return value to include options_rows and wire it into the endpoint response, then restart:


5 actions
Two edits in parallel — update the parser's return value and the endpoint response, then restart:


5 actions
Server is up — let me run the smoke test now with a realistic CSV that has all the options patterns:


2 actions
All four options detection patterns working perfectly:

Action keywords: Buy to Open, Sell to Close, Buy to Close, Sell to Open, Expired, Assigned, Buy to Close — all 7 caught, zero leaking into equity positions
OCC symbol format: AAPL240119C00180000 — caught by pattern
Fidelity dash prefix: -AMZN240119C00170000 — caught immediately
Schwab embedded date: NVDA 01/19/2024 500.00 C — caught by space detection
In every case, the underlying equity (AAPL, GOOGL, AMZN, NVDA, MSFT) is correctly imported as a stock holding, and options rows are stripped out silently with a count and detail list the frontend can surface in the import review.

The endpoint now returns options_skipped (count) and options_detail (up to 20 rows, with ticker, action, and reason) alongside the existing skipped_detail. The frontend can show something like "10 equity lots imported · 7 options transactions skipped" in the import confirmation dialog.


3 actions
The options handling is live and tested. Here's a summary of exactly what the CSV importer now does with options transactions:

Detected and skipped — never imported as equity positions:

Pattern	Example	Brokerage
Action keyword	Buy to Open, Sell to Close, Sell to Open, Buy to Close	Schwab, TD, Fidelity, IBKR
Lifecycle action	Expired, Assigned, Exercised	All
OCC symbol format	AAPL240119C00180000	Most brokerages
Dash prefix symbol	-AAPL240119C00180000	Fidelity
Embedded date in symbol	AAPL 01/19/2024 180.00 C	Schwab
Instrument type column	Column says "Option"	IBKR, some others
What comes back in the response:

options_skipped — count of options rows dropped
options_detail — list of {ticker, action, reason} for each one, so the frontend can show something like "7 options transactions were skipped — only equity positions are tracked" in the import review dialog
The underlying stock positions (AAPL shares, MSFT shares, etc.) still import correctly even when the same ticker has both equity and options transactions in the same CSV.