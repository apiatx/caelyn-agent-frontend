You are working in this repo only:

* Frontend repo: https://github.com/apiatx/caelyn-agent-frontend.git

Workflow rules:

* Work directly on `main`
* Do NOT create a branch
* Do NOT create a PR
* Commit directly to `main`
* Push directly to `main`

This is a targeted frontend follow-up. Do not broaden scope.

PRIMARY GOALS

1. Options Flow page should always show the best available cached data on revisit
2. Home Unusual Options Flow should reflect the repaired backend’s cross-category unusualness ranking
3. The UI must clearly distinguish:

* live/fresh
* stale but usable
* refreshing
* warming
* true zero results

AUDIT FIRST
Inspect the current frontend implementation and determine:

* whether the new `/api/options/all-tabs` payload is being consumed directly or partially
* whether the unified 2x2 panel page is still allowing categories to appear blank when stale cached data exists
* whether the page is properly rendering snapshot metadata per category
* whether Home unusual options is still wired to an old row shape or limited subset

IMPLEMENT
A. Options Flow page

* for each category panel, always render rows if rows exist
* if stale but usable, still render with stale/refreshing status
* do not visually blank the panel during recompute
* surface:

  * updated_at
  * data_state
  * refresh_in_progress
  * result_count
    if backend provides them

B. Home page

* wire the Home unusual options section to the repaired backend output
* make sure it is not visually biased by old assumptions like “top megacap list”
* render the returned rows honestly

C. Empty-state honesty

* only show “no unusual options activity” on explicit true-zero state
* if backend says warming/refreshing/no_data_yet with stale snapshot available, render the snapshot

VERIFY
Before finishing, verify:

1. Options Flow page shows cached data immediately on revisit when available
2. panels do not go blank during refresh if rows exist
3. Home unusual options reflects the repaired backend ranking
4. no new fetches or polling hacks were added
5. query count did not meaningfully increase

DELIVERABLES
When done, report:

1. exact frontend files changed
2. exact previous frontend behavior that made the page feel blank
3. what metadata/status states are now rendered
4. whether Home row rendering changed
5. whether query count changed
6. confirmation changes were committed and pushed directly to `main`

EXECUTION STYLE

* Audit first
* Keep the UI honest
* Prefer stale-but-usable over blank
* Then commit and push directly to `main`
