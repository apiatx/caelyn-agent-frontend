Workflow rules:

Work directly on main
Do NOT create a branch
Do NOT create a PR
Commit directly to main
Push directly to main

This is a focused Social page data-path audit/fix. Do not broaden scope.

CURRENT FACTS
Backend now claims the Social response preserves the existing flat consensus payload AND adds new sibling keys:

freshest_alpha
theme_leadership
sentiment_acceleration

Backend explicitly reported:

freshest_alpha.trades: 3
theme_leadership.themes: 4
sentiment_acceleration: 10
with the old flat consensus keys still intact.

Yet the UI still shows:

“No fresh alpha data yet”
“No theme data yet”
“No acceleration data yet”

So we need to prove exactly where the data is being lost.

AUDIT FIRST — NO GUESSING

Open browser devtools on the Social page
Inspect the exact live network response for the page’s Social data request
Determine:
what exact URL/path the frontend is hitting
whether that response contains:
freshest_alpha
theme_leadership
sentiment_acceleration
Compare the actual live JSON to the render logic in onchain-social.tsx

I want a precise answer to:

is the browser receiving those keys and failing to render them?
OR
is the frontend proxy/query still serving the wrong/old payload?

CHECK THESE SPECIFIC FAILURE MODES

A. Wrong route / wrong proxy

confirm the Social page is actually hitting the intended Social dashboard route/proxy
not the Home trending_on_x subset route
not an old cached fallback route

B. Wrong live response shape

if the browser response lacks the 3 sibling keys, identify exactly where the wrong response is coming from:
frontend proxy route
stale edge/server cache
wrong backend URL mapping

C. Wrong render path
If the browser response DOES include the 3 keys:

verify exact paths used by the renderer:
freshest_alpha.trades
theme_leadership.themes
sentiment_acceleration
verify no over-defensive empty guards are hiding data

D. Query cache / stale client data

confirm whether React Query or equivalent is serving stale data from before the backend merge
invalidate/fix if needed

IMPLEMENT
Fix only the real drop point.

Possible valid fixes:

correct frontend proxy mapping
correct query URL
invalidate stale cache
correct field paths
remove over-aggressive empty guards

Do NOT

do not guess
do not redesign the page
do not change the working existing consensus section
do not add new fetches unless the current fetch is provably wrong

VERIFY
Before finishing, report:

the exact request URL the browser is making
whether the live browser response contains:
freshest_alpha
theme_leadership
sentiment_acceleration
the exact root cause:
wrong route
wrong proxy mapping
stale client cache
wrong field path
over-aggressive empty guard
proof the 3 sections now render when those keys are present
exact frontend files changed