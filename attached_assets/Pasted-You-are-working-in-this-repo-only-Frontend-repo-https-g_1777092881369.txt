You are working in this repo only:

* Frontend repo: https://github.com/apiatx/caelyn-agent-frontend.git

Workflow rules:

* Work directly on `main`
* Do NOT create a branch
* Do NOT create a PR
* Commit directly to `main`
* Push directly to `main`

This is a focused bug fix. Do not broaden scope.

PRIMARY BUG
The Social page manual refresh now works, but the page still says:

`No fresh alpha data yet.`

I need you to determine whether:

1. the backend live response truly has no fresh alpha trades
   OR
2. the frontend is reading the wrong field path / wrong shape

IMPORTANT BACKEND CONTRACT
The backend said the Social X dashboard returns:

* `freshest_alpha` as an object
* shape:

  * `freshest_alpha.trades[]`
  * `freshest_alpha.spotlight`
    It is NOT just a plain array.
    The backend also previously verified that the snapshot used in testing had `raw.fresh_trades` populated.  

AUDIT FIRST — NO GUESSING

1. Open browser devtools on the Social page
2. Inspect the actual live network response for `/api/social/x-dashboard`
3. Check these exact paths:

   * `freshest_alpha`
   * `freshest_alpha.trades`
   * `freshest_alpha.trades.length`
   * `freshest_alpha.spotlight`
4. Compare those exact live payload values to the current render logic on the Social page
5. Determine the exact root cause:

   * backend returned empty `freshest_alpha.trades`
   * OR frontend is checking the wrong path
   * OR frontend is using stale query data
   * OR a type mismatch / guard is suppressing render

VERY IMPORTANT
Do not assume the bug.
Prove it by comparing:

* actual live network payload
  vs
* exact render conditions

IMPLEMENT
Fix only what is needed.

Possible likely fix:

* if the UI is checking `freshest_alpha.length` or similar, update it to use `freshest_alpha.trades`
* if the UI ignores spotlight when trades exist, keep spotlight as supplemental but do not treat it as the primary list
* if the UI is over-guarding on empty state, fix the guard

RENDERING EXPECTATION
If `freshest_alpha.trades.length > 0`, render the section.
Only show “No fresh alpha data yet” if:

* `freshest_alpha.trades` is truly empty in the live backend response

VERIFY
Before finishing, verify:

1. the live network response is inspected and reported
2. if trades exist in payload, they now render
3. “No fresh alpha data yet” only appears on true empty payload
4. no unrelated Social sections are broken

DELIVERABLES
When done, report:

1. exact frontend files changed
2. exact live payload values observed for `freshest_alpha`
3. exact root cause
4. whether it was wrong field path, stale data, or true empty backend payload
5. confirmation changes were committed and pushed directly to `main`
