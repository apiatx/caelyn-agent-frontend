Continue working in the frontend repo only:

* Frontend repo: https://github.com/apiatx/caelyn-agent-frontend.git

Workflow rules:

* Work directly on `main`
* Do NOT create a branch
* Do NOT create a PR
* Commit directly to `main`
* Push directly to `main`

This is the real Options Screener UI transition. The backend is now ready.

Important backend handoff:

* A direct master screener endpoint already exists:

  * `GET /api/options/screener`
* Supported query params:

  * `asset_type=etf|stock`
  * `market_cap_bucket=megacap|large|small|etf|unknown`
  * `limit=N`
* Payload already includes:

  * ranked `tickers`
  * `all_contracts`
  * `data_state`
  * `result_count`
  * `updated_at`
  * `source`
  * `refresh_in_progress`
  * `stale`
  * `cache_age_seconds`
  * `next_refresh_in_seconds`
* Each ticker row already includes:

  * `ticker`
  * `composite_score`
  * `asset_type`
  * `market_cap_bucket`
  * `underlying_price`
  * `price_change_pct`
  * `call_volume`
  * `put_volume`
  * `total_volume`
  * `pc_ratio`
  * `call_oi`
  * `put_oi`
  * `total_oi`
  * `avg_call_iv`
  * `avg_put_iv`
  * `iv_skew`
  * `max_pain`
  * `primary_signal`
  * `confidence`
  * `confidence_score`
  * `modular_scores`
  * `thesis`
  * `risks`
  * `top_contracts`
  * `top_calls`
  * `top_puts`
  * `expiration_focus`
  * `stock_context`
  * `options_context`
  * `data_quality`
  * `data_source`
* Existing drilldown endpoints remain compatible via:

  * row `ticker`
  * `top_contracts[].contract_symbol`
* `/api/options/all-tabs` still exists, but it is no longer the primary frontend data source.
  Use `/api/options/screener` as the primary source of truth now. 

PRIMARY GOAL
Replace the current 4-panel Options Flow page with a true **single master unusual-options screener**.

I do NOT want the primary UX to remain:

* ETFs panel
* Megacaps panel
* Large Caps panel
* Small Caps panel

I DO want:

* one master ranked unusual-options screener
* sortable by meaningful fields
* filter chips/toggles for:

  * All
  * Stocks
  * ETFs
  * Megacap
  * Large
  * Small
* preserve ticker click -> popup/detail modal behavior

AUDIT FIRST
Inspect the current Options page and identify:

1. where it still renders the 4 category panels
2. what parts of the current row and detail modal can be reused
3. what existing sorting/table/list utilities already exist
4. how the current detail modal is opened and what fields it needs

IMPLEMENT

PART 1 — Replace 4-panel layout

* Remove the 4 bucket panels as the primary layout
* Build one master screener list/table/grid from `/api/options/screener`
* Default sort:

  * `composite_score` descending
* Keep the page dense and useful like a screener, not a dashboard

PART 2 — Filters
Add top-level filters/chips/toggles for:

* All
* Stocks
* ETFs
* Megacap
* Large
* Small

These should filter the same master list, not swap page sections.

PART 3 — Sorting
Support client-side sorting by at least:

* composite score
* ticker
* asset type
* market cap bucket
* price change %
* P/C ratio
* total volume
* total OI
* confidence score
* maybe max pain / IV skew if it remains readable

Pick the best set for screener usability.

PART 4 — Row design
Each row should expose the most useful screener data compactly, such as:

* rank
* ticker
* asset type
* market cap bucket
* primary signal
* composite score
* price change %
* P/C ratio
* volume / OI context
* confidence badge
* short thesis or compact context snippet

Do not make it bloated, but do make it feel like a real screener.

PART 5 — Preserve detail modal
Critical:

* clicking a row must still open the richer detail modal/popup
* keep charts, contract drilldown, context, and existing deeper data UX intact
* preserve compatibility with current ticker/detail flow

PART 6 — State semantics
At the page level, surface backend state cleanly:

* live_ok
* stale_but_available
* refresh_in_progress
* no_data_yet
* true_zero_results

Do not go back to blank/ambiguous state messaging.

PART 7 — Home page
Do NOT redesign Home in this pass unless needed for row-shape compatibility.
Only touch Home if the unusual options section needs to align with the master screener row shape or modal compatibility.

VERIFY
Before finishing, verify:

1. the 4-panel layout is no longer the primary UX
2. `/api/options/screener` is the primary data source
3. filters for Stocks/ETFs/cap buckets work
4. sorting works
5. row click -> detail modal still works
6. query count is reasonable
7. no unnecessary fetch fan-out was added

DELIVERABLES
When done, report:

1. exact frontend files changed
2. exact old layout removed
3. exact new data source used
4. what filters were added
5. what sorting options were added
6. confirmation detail modal still works
7. whether Home needed any compatibility updates
8. whether query count changed
9. confirmation changes were committed and pushed directly to `main`

EXECUTION STYLE

* Audit first
* Build the actual screener UX now
* Reuse the existing detail modal
* Stop treating category buckets as the primary page structure
* Then commit and push directly to `main`
