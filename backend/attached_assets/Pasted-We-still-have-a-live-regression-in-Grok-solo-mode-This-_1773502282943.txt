We still have a live regression in Grok solo mode. This is a targeted bug-fix pass only. Do NOT refactor the architecture. Do NOT remove or simplify existing proprietary data gathering. Do NOT change Caelyn routing except where necessary to restore correct behavior.

Observed failure:
When Grok is selected as the solo model and the user triggers "Trending Now", the system returns a generic freeform response labeled like "Best Trades" instead of the correct structured Trending Now preset output.

This proves two regressions still exist in Grok solo mode:
1. Grok solo is not reliably honoring preset_intent / INTENT_PROFILE resolution
2. Grok solo is not reliably returning the structured preset response contract

Required product behavior:
When a direct solo model is selected from the top row (Claude / ChatGPT / Grok / Gemini / Perplexity), the selected model must run the FULL preset flow, not a generic chat flow.

That full solo flow must be:

1. preserve preset_intent if present
2. resolve the correct INTENT_PROFILE / display_type for that preset
3. run the same prompt orchestration and query_info planning used by preset buttons
4. run the same proprietary/custom data gathering pipeline already defined for that preset
5. build the full final system prompt via _build_prompt using the correct INTENT_PROFILE
6. call the selected solo model as the final model
7. return the correct structured preset response

CRITICAL:
Grok solo must NOT:
- infer a different preset semantically from raw text
- bypass the preset contract
- behave like a collaborator
- return freeform analysis text when a preset/display contract exists

Specifically for this bug:
If the request is for Trending Now, Grok solo must resolve and render the Trending Now route/profile, not Best Trades or any other semantically similar route.

Audit and fix the exact failure path:
1. Inspect how the frontend request arrives for this case:
   - what payload is sent when Grok is selected and "trending now" is triggered
   - whether preset_intent is present or missing
   - whether the backend is falling back to query text classification
2. Inspect the solo Grok path:
   - whether it is preserving preset_intent
   - whether it is loading the correct INTENT_PROFILE
   - whether _build_prompt is using the correct category/display contract
   - whether the returned response is still going through the structured preset response path
3. If preset_intent is missing for solo prompt text like "trending now", do NOT let Grok free-associate to a different preset.
   Instead:
   - either normalize the text to the correct preset_intent before final dispatch
   - or preserve the exact preset route from the UI if it exists
   - or use the same preset/command resolution logic that other solo models used before regression

4. Ensure Grok solo final path uses:
   - XaiSentimentProvider
   - full final system prompt from _build_prompt
   - full proprietary data pipeline
   - full preset response contract

5. Ensure Grok collaborator mode remains separate:
   - collaborator mode = short focused findings
   - solo mode = full preset response

Important:
Do NOT just patch Trending Now only.
Fix the root cause so Grok solo behaves correctly for all preset routes.

What I want before coding:
1. exact cause of why Grok solo resolved Trending Now as Best Trades
2. exact cause of why Grok solo returned non-JSON / non-structured output
3. minimum-change fix plan
4. exact files to modify

What I want after coding:
1. exact files changed
2. exact cause of the wrong preset resolution
3. exact cause of the non-structured output
4. confirmation that Grok solo now preserves preset_intent / INTENT_PROFILE
5. confirmation that Grok solo now returns the structured preset response contract
6. proof traces for:
   - Grok solo + Trending Now
   - Grok solo + Best Trades
   - Grok solo + one more preset route
For each proof trace show:
- inbound payload
- resolved preset_intent
- resolved INTENT_PROFILE / display_type
- proprietary data pipeline used
- final model path used
- whether X search / web search were enabled
- whether structured preset output was returned