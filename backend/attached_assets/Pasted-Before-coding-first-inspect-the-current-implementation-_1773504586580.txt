Before coding, first inspect the current implementation and show me the minimum-change plan, including the exact files you expect to modify.

We need a targeted backend fix + feature pass for chat history. Do NOT refactor the whole app. Do NOT change existing proprietary data pipelines except where needed to support the new history performance feature. Keep this low-risk and incremental.

There are 3 goals:

1. Fix history persistence/rendering support so saved responses reliably appear again in the frontend History page.
2. Support cleaner section naming/order for history categories.
3. Add deterministic portfolio gain/loss tracking for each saved history response, without using AI or paid API calls beyond our already-configured proprietary/provider stack.

CRITICAL PRODUCT REQUIREMENTS

A) History persistence
The app is connected to Neon correctly, but history is not consistently appearing in the frontend History page. Audit the full history write/read path and fix the regression.

B) Category naming/order
The history sections should be ordered and labeled like this:
1. Terminal Chat
2. Overview
3. Trades & Ideas
4. Fundamental
5. Sectors
6. TA Screener
7. Earnings
8. Prediction Markets
9. NotifAI

Notes:
- "Terminal" should be at the top and should be renamed to "Terminal Chat"
- "Earnings Agent" should be renamed to "Earnings"

C) History performance metric
For each saved history response, calculate a portfolio gain/loss % based on the tickers recommended in that response.

Business logic for performance:
- Treat each saved response as a basket/portfolio of the tickers mentioned or recommended in that response.
- Assume purchase at the time the response was generated.
- Show current basket performance % as of now.
- Use deterministic logic only. Do NOT use AI for ticker extraction or scoring.
- Avoid any new paid APIs if possible; use our existing data providers and proprietary data stack.

IMPLEMENTATION REQUIREMENTS

PART 1 — Audit and fix history persistence
1. Inspect the exact write path for:
   - preset responses
   - free-form terminal chat responses
   - solo model responses
   - Caelyn responses
2. Inspect the exact read path used by the frontend History page.
3. Identify why history entries are no longer showing consistently.
4. Fix the smallest root cause possible.
5. Confirm Neon-backed persistence is being used and not bypassed.

PART 2 — Section/category normalization for history
1. Add or update a central normalization/mapping layer for history categories.
2. Normalize existing history entries into these display categories:
   - terminal_chat -> "Terminal Chat"
   - overview -> "Overview"
   - trades_ideas -> "Trades & Ideas"
   - fundamental -> "Fundamental"
   - sectors -> "Sectors"
   - ta_screener -> "TA Screener"
   - earnings -> "Earnings"
   - prediction_markets -> "Prediction Markets"
   - notifai -> "NotifAI"
3. Ensure "Terminal Chat" is used for free-form query responses from the main agent chatbar on the Terminal page.
4. Ensure "Earnings Agent" is renamed to "Earnings" in the history data returned to frontend.
5. Return history grouped/sortable in the target order above.

PART 3 — Add deterministic portfolio performance for each saved response
We want each saved response to have a basket performance %.

Requirements:
1. Extract recommended symbols/tickers from each saved response using deterministic rules only.
2. Prefer structured response fields first, then deterministic fallbacks.
3. Do NOT use LLMs/AI for extraction.

Preferred extraction order:
- structured recommendation cards / idea objects / trade cards / watchlist items
- explicit ticker arrays already in saved payloads
- deterministic parsing fallback from known fields only
- regex fallback as last resort

For each saved response, store or derive:
- response_id
- created_at timestamp
- normalized category
- extracted tickers/symbols
- asset type if inferable (equity, crypto, commodity/futures, ETF, etc.)
- entry price snapshot per symbol
- latest price per symbol
- basket return %
- optional per-symbol return %

Portfolio performance rules:
- Default to equal-weight across extracted tickers
- If no valid tickers are extracted, performance should be null / unavailable
- If some symbols fail to price, use the successfully priced symbols and report count
- Entry price should be the closest available price at/after response creation time
- Latest price should be current/latest available
- Basket return = mean of per-symbol returns unless later weighting rules are added

Use existing providers only where possible:
- equities/ETFs: existing Finnhub / FMP / Polygon / TwelveData paths
- crypto: existing CoinGecko / CMC / Hyperliquid / Artemis paths
- commodities/futures: existing provider logic already used in the app for those instruments
Do not add new paid APIs just for this.

Important design requirement:
- Keep this deterministic and maintainable
- Add a small history-performance service/helper rather than burying logic in a giant monolith
- Cache or persist computed entry prices so we are not recomputing everything expensively on every page load
- It is acceptable to compute current/latest price on read, but entry snapshot logic should be persisted if practical

Recommended implementation shape:
- add a small helper/service for history symbol extraction and performance calculation
- add DB fields or a companion table for history positions/performance snapshots if needed
- preserve backward compatibility with existing history records where possible
- for old records with no stored tickers, attempt deterministic backfill from saved payloads

PART 4 — API response for frontend
Update the history API so each history item/group can include:
- normalized display category
- display order key
- entry count
- basket_performance_pct
- basket_performance_label
- priced_symbol_count / total_symbol_count
- maybe a small status field like "priced", "partial", "unavailable"

The frontend should not have to compute the performance itself.

PART 5 — Verification
After implementing, provide:
1. exact files changed
2. exact root cause of the history persistence regression
3. exact fix for the read/write path
4. exact category normalization mapping used
5. how Terminal Chat entries are identified and saved
6. how ticker extraction works
7. how entry prices are determined
8. how latest prices are determined
9. how basket performance is calculated
10. any limitations / edge cases

Also provide proof with at least 3 sample history items:
- one Terminal Chat item
- one preset response with tickers
- one item with no valid tickers
For each, show:
- normalized category
- extracted symbols
- entry timestamp
- entry prices
- latest prices
- computed basket return or unavailable state

Important constraints:
- Do NOT use AI/LLM extraction
- Do NOT break current response saving
- Do NOT do a large refactor
- Keep it low-risk and incremental