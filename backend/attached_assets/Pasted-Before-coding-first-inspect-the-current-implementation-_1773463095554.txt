Before coding, first inspect the current implementation and show me the minimum-change plan, including the exact files you expect to modify.

I want to implement Phase 1: Routing MVP for Caelyn mode.

This is NOT the full modular refactor. Do NOT rebuild the whole architecture. Do NOT introduce YAML yet unless absolutely necessary. Do NOT rename model/provider internals to branded names yet. Keep real internal model IDs like claude, grok, gemini, perplexity, chatgpt.

CRITICAL PRODUCT REQUIREMENT:
I already have an existing screen-specific / prompt-specific custom data pipeline that pulls structured market and financial data from multiple APIs and internal engines. This is extremely important and must NOT be altered, bypassed, degraded, simplified, or replaced by the new Caelyn routing logic.

Examples of existing data/providers that may already be used depending on the prompt/screen:
- Finviz
- Finnhub
- FMP
- FRED
- EDGAR
- Hyperliquid
- Polymarket
- Artemis
- CMC
- CoinGecko
- Altfins
- AlphaVantage
- TwelveData
- PolygonMassive
- plus existing custom ticker details, financials, earnings/catalysts, technical indicators, futures, open interest, prediction market odds, regime, screener logic, and other proprietary context already assembled by the current system

This proprietary/custom data gathering layer is AUTHORITATIVE and should remain exactly as-is unless a tiny compatibility adjustment is required.

Goal:
Implement deterministic, category-based routing for the automatic "Caelyn" mode only, while preserving the existing prompt-specific data assembly pipeline.

Very important architectural rule:
For Caelyn mode, the flow should be:

1) determine prompt category / route
2) preserve and run the existing screen-specific / prompt-specific custom data gathering pipeline exactly as it works today
3) optionally gather collaborator model outputs according to the routing matrix
4) send BOTH:
   - existing custom/proprietary data context
   - collaborator outputs
   to the final reasoning model
5) return the final response

This should NOT become a new flow that bypasses the current custom data pipeline.

Another important rule:
- Existing prompt-specific provider/data selection should continue to determine which APIs/data are included for each screen/prompt.
- Do NOT inject heavy proprietary data into prompts/screens that currently do not need it.
- Example: "Headlines" may remain lightweight if that is how it already works.
- But for prompts that currently rely on custom data, keep that data fully included.

Product logic:
- "Caelyn" mode should automatically choose:
  1) final reasoning model
  2) collaborator models
  3) analysis depth/mode
  4) optional response template identifier
- "Customize" / manual mode should continue to work as-is and should NOT be overridden by this routing logic.

Important:
This should be a hardcoded routing matrix MVP with minimum blast radius.
Do not attempt a full architecture rewrite.
Do not break existing direct model selection.
Do not break custom collaboration mode.
Do not rewrite or replace current provider selection and evidence assembly logic.

Core implementation requirements:
1. Add a central routing table / mapping for Caelyn automatic mode.
2. Route based on category / preset / command / selected prompt family, using the existing frontend request data if available.
3. If the user is in Caelyn mode, use the routing table below.
4. If the user is NOT in Caelyn mode, preserve existing behavior.
5. Keep backward compatibility with old request payloads when possible.
6. If there is no recognized category, fall back safely to a sensible default:
   - final model: claude
   - collaborators: ["grok", "perplexity"]
   - mode: standard

Implement this exact routing matrix for Caelyn mode:

OVERVIEW
- Daily Briefing -> collaborators ["perplexity", "grok"], final "claude", mode "standard"
- Macro Overview -> collaborators ["gemini", "perplexity"], final "claude", mode "deep"
- Headlines -> collaborators [], final "perplexity", mode "fast"
- Upcoming Catalysts -> collaborators ["perplexity"], final "claude", mode "fast"
- Trending Now -> collaborators ["perplexity"], final "grok", mode "fast"
- Social Momentum -> collaborators [], final "grok", mode "fast"
- Sector Rotation -> collaborators ["gemini", "perplexity"], final "claude", mode "deep"

TRADES & IDEAS
- Best Trades -> collaborators ["grok", "perplexity"], final "claude", mode "standard"
- Best Investments -> collaborators ["gemini", "perplexity"], final "claude", mode "standard"
- Asymmetric R:R -> collaborators ["gemini", "grok"], final "claude", mode "standard"
- Small Cap Spec -> collaborators ["grok", "perplexity"], final "claude", mode "standard"
- Short Squeeze -> collaborators ["grok", "perplexity"], final "claude", mode "fast"

FUNDAMENTAL
- Fundamental Leaders -> collaborators ["gemini"], final "claude", mode "standard"
- Rapidly Improving -> collaborators ["gemini", "perplexity"], final "claude", mode "standard"
- Earnings Watch -> collaborators ["perplexity", "gemini"], final "claude", mode "standard"
- Insider Buying -> collaborators ["perplexity"], final "claude", mode "fast"
- Revenue Reaccelerating -> collaborators ["gemini"], final "claude", mode "standard"
- Margin Expansion -> collaborators ["gemini"], final "claude", mode "standard"
- Undervalued Growth -> collaborators ["gemini", "perplexity"], final "claude", mode "standard"
- Institutional Accumulation -> collaborators ["perplexity", "gemini"], final "claude", mode "standard"
- Free Cash Flow Leaders -> collaborators ["gemini"], final "claude", mode "standard"

SECTORS
- Crypto -> collaborators ["grok", "perplexity"], final "claude", mode "standard"
- Commodities -> collaborators ["perplexity", "gemini"], final "claude", mode "standard"
- Energy -> collaborators ["perplexity", "gemini"], final "claude", mode "standard"
- Materials -> collaborators ["gemini", "perplexity"], final "claude", mode "standard"
- Aerospace/Defense -> collaborators ["perplexity", "gemini"], final "claude", mode "standard"
- Tech -> collaborators ["gemini", "perplexity"], final "claude", mode "standard"
- AI/Compute -> collaborators ["grok", "gemini", "perplexity"], final "claude", mode "deep"
- Quantum -> collaborators ["grok", "gemini", "perplexity"], final "claude", mode "deep"
- Fintech -> collaborators ["gemini", "perplexity"], final "claude", mode "standard"
- Biotech -> collaborators ["perplexity", "gemini"], final "claude", mode "standard"
- Real Estate -> collaborators ["gemini", "perplexity"], final "claude", mode "standard"

Implementation details:
1. First inspect the current request flow and identify:
   - where selected model/mode is determined
   - where collaboration agents are chosen
   - where preset/category/command data is available
   - where final model execution happens
   - where existing prompt-specific custom data/provider gathering happens
   - where the assembled proprietary/custom evidence is passed into the reasoning model today

2. Then implement the smallest clean routing layer possible.

3. Prefer creating a new focused helper/module for this routing logic instead of burying it deeper into a huge monolith file.
   Examples of acceptable new files:
   - agent/caelyn_routing.py
   - agent/routing.py
   - config/caelyn_routes.py

4. Normalize category names so small variations do not break routing.
   For example:
   - "Daily Briefing", "daily briefing", "/briefing", "briefing"
   should resolve consistently if possible.

5. Preserve existing custom/manual collaboration logic.
6. Preserve existing single-model direct selection logic.
7. Caelyn mode should only auto-route when the active top-level model/mode is Caelyn.

8. CRITICAL:
   The new routing layer must be additive to the existing custom data pipeline.
   That means:
   - do not remove any existing data-provider calls for a route that already uses them
   - do not replace existing prompt-specific evidence assembly with collaborator model outputs
   - do not downgrade structured data gathering into generic LLM summarization
   - do not move core custom data inclusion behind a collaborator toggle
   - keep the current prompt/screen-specific evidence-building logic authoritative

9. If needed, introduce a small internal structure for the final synthesis input that clearly combines:
   - user request
   - existing proprietary/custom data context
   - collaborator findings
   - route metadata
   But do this with minimum blast radius.

10. If a route currently does not use heavy proprietary data, keep that behavior unless the existing implementation already includes it.
    In other words: preserve current data inclusion behavior by prompt/screen rather than forcing every route to use every data source.

Important behavior rules:
- Collaborator outputs should remain collaborators only; do not let this MVP create recursive or uncontrolled multi-agent loops.
- Keep existing execution flow if possible: existing custom data assembly first (or as currently implemented), then collaborator outputs, then final model synthesis.
- If the current code executes collaborators sequentially, keep that for now unless parallelizing is already easy and low-risk.
- Do not redesign prompts yet unless necessary for this routing to work.
- Do not implement branded names like Beacon/Pulse/Atlas/Sage yet in backend logic.

What I want from you:
1. Audit the backend flow first and explain the minimum-change implementation plan
2. Explicitly identify where the current proprietary/custom data assembly occurs and how you will preserve it unchanged
3. Implement the routing MVP
4. Tell me exactly:
   - which files were changed
   - what new helper/module was added
   - how Caelyn mode is detected
   - how category normalization is handled
   - how the existing custom data pipeline was preserved
   - whether collaborator outputs are additive to the custom data context
   - what fallback behavior is used
   - what response metadata was added, if any
5. Call out any weak spots caused by the current monolith and what should be deferred to later refactor phases

Success criteria:
- Caelyn mode automatically uses the exact route-by-category matrix above
- Existing proprietary/custom prompt-specific data gathering remains intact
- Collaborator models supplement that data instead of replacing it
- Manual Customize mode still works
- Direct single-model mode still works
- No major refactor yet
- Minimal blast radius