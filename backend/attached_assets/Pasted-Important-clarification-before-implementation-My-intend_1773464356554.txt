Important clarification before implementation:

My intended architecture is:

1. The final reasoning model is the model that writes the final response.
2. In product terms, this is the "reasoning model."
3. If the current codebase uses `primary_model` to represent the actual final model inside `agent_collab`, that is fine for compatibility, but please treat `primary_model` as the effective final reasoning model and document that clearly.

Second clarification:
The collaborator models are not only meant to participate in synthesis after the proprietary data pipeline.
They are also meant to gather the types of external information they are best at gathering, and then pass their findings/opinions to the final reasoning model.

Desired collaborator behavior in Caelyn mode:
- Grok: gather social sentiment / narrative / trend context
- Perplexity: gather current news / catalysts / recent developments
- Gemini: gather broader web research / contextual background
- Final reasoning model: synthesize proprietary structured data + collaborator findings + user request

Critical constraint:
- Do NOT alter or remove my existing proprietary/custom data gathering pipeline.
- Keep `_gather_data_safe` and all current screen-specific provider logic untouched.
- Collaborator gathering should be additive to that pipeline, not a replacement.

So the desired flow is:

existing proprietary/custom data gathering
+ collaborator-specific retrieval/findings
+ final reasoning model synthesis

Please update the implementation plan so that Caelyn collaborators do real collaborator-specific information gathering/retrieval where possible, not just post-hoc commentary on the already-assembled data.

However, keep the implementation minimal and low-risk:
- reuse any existing collaborator call patterns if possible
- do not do a large refactor
- do not break Customize/manual mode
- do not break solo/direct model mode

Before coding, please explain:
1. how you will represent the final reasoning model cleanly
2. how each collaborator will gather its own relevant information in addition to receiving existing proprietary context
3. how you will keep `_gather_data_safe` fully intact
4. what the minimum-change implementation looks like