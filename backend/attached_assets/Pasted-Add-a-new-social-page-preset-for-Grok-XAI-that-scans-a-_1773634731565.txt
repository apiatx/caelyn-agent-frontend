Add a new social-page preset for Grok/XAI that scans a fixed curated list of selected X traders, separate from the existing broader "top X traders" consensus preset.

You only have access to the BACKEND repo.

Important constraints:
- Do NOT break the existing preset:
  "Consensus tickers among top X traders"
- Do NOT repurpose the existing backend preset if it currently powers the broader/popular-X consensus behavior.
- Create a separate new preset for the curated account list below.
- Keep changes minimal and targeted.
- Use the existing Grok/XAI social-page preset flow where possible.

## Problem to solve
The existing preset/button "Consensus tickers among top X traders" is returning a broader consensus based on large/popular X accounts, not the curated trader list I actually want.

That existing behavior is fine — keep it.

I now want a second distinct preset:
"Concensus tickers among select X traders"

This new preset must scan ONLY the curated X accounts listed below and return the consensus tickers and thesis based specifically on them.

## Curated account list to use for the new preset
Use this exact account set for the new preset:

- @StockSavvyShay
- @HyperTechInvest
- @crux_capital_
- @SJCapitalInvest
- @BlackPantherCap
- @Kaizen_Investor
- @Venu_7_
- @CKCapitalxx
- @TheTape_TNM
- @equitydd
- @Speculator_io
- @DrJebaim
- @StonkValue
- @stamatoudism
- @yianisz
- @sunxliao
- @futurist_lens
- @Thomas_james_1

## What to implement
Create a new backend preset key specifically for this curated-list scan.

Suggested canonical key:
- x_select_trader_consensus

You may use another project-consistent key if needed, but it must be distinct from the existing broader trader-consensus preset.

## Required behavior of the new preset
For this new preset only:
- use Grok/XAI
- scan only the curated 18 X accounts above
- analyze posts from the last 30 days
- determine the consensus tickers among those selected traders
- synthesize the thesis for each ticker
- explain what they are most bullish on
- identify what they increasingly agree on
- provide a final opinion on the strongest names to consider buying

## Output structure
Return the same general structured response style used for trader-consensus social responses, including fields like:
- display_type
- scan_type
- title
- analysis_window
- accounts_analyzed
- consensus_summary
- consensus_tickers
- momentum_leaders
- early_vs_crowded
- final_opinion

But for this new preset:
- accounts_analyzed must reflect the curated 18-account list
- all consensus/thesis/ranking must be derived only from those selected accounts

## Important rules
- Do NOT change the behavior of the existing broader "top X traders" preset.
- Do NOT merge these two presets into one.
- The new preset must be separate and callable independently.
- Reuse as much of the existing Grok consensus logic as possible, but isolate the account universe for this new preset.
- Keep response rendering compatibility with the current social structured-response frontend.

## Deliverables
1. Implement the new backend preset.
2. Show exact files changed.
3. Show the new preset key.
4. Explain how it differs from the existing broader trader-consensus preset.
5. Confirm that the curated 18-account list is the only account universe used by the new preset.
6. Show an example response shape for the new preset.

## Acceptance criteria
The task is done when:
- the existing "Consensus tickers among top X traders" preset still works unchanged
- a new distinct backend preset exists for the curated 18-account list
- the new preset scans only those 18 accounts
- it returns consensus tickers and thesis based on those selected traders only
- no other social presets are broken

Before coding, first audit the current existing trader-consensus preset and confirm whether it is currently wired to a broader account universe. Then add a separate curated-list preset with the minimum necessary changes.