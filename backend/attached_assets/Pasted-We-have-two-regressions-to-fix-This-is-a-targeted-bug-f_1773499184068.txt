We have two regressions to fix. This is a targeted bug-fix pass only. Do NOT refactor the architecture. Do NOT remove or simplify existing proprietary data gathering. Do NOT change Caelyn routing except where necessary to restore correct behavior.

We must preserve:
- preset-specific proprietary data gathering
- per-prompt response formatting / display_type contracts
- all solo model functionality
- all Caelyn functionality that already works

BUG 1 — Trending Now / commodities / TradingView chart symbols are wrong
Symptoms:
- The rendered card content and the embedded TradingView chart symbol are out of sync.
- Example: card text references crude oil / gold futures, but the TradingView widget loads an unrelated symbol.
- This worked previously and regressed.

Task:
1. Audit the full path for how chart symbols / tradingview symbols are produced for preset responses, especially Trending Now and commodities-related outputs.
2. Identify where the correct chart symbol used to be attached to each card/item and where that mapping is now being lost, overwritten, or defaulted incorrectly.
3. Restore the previous behavior so each rendered card uses the correct TradingView symbol for its actual instrument.
4. Verify specifically for:
   - commodities / futures symbols like CL1!, GC1!, etc.
   - any route where cards include tradingview_url or TradingView symbol metadata
5. Do NOT hardcode a one-off fix for only one symbol. Fix the response-building path correctly.

Expected outcome for Bug 1:
- If a card is about CL1! WTI Crude Oil, its TradingView widget/chart metadata must point to the correct CL1! / corresponding TradingView symbol.
- If a card is about GC1! Gold, its chart metadata must match GC1! / correct TradingView symbol.
- Card metadata and widget metadata must stay aligned.

BUG 2 — Solo model mode is broken, especially Grok
Symptoms:
- When selecting a solo model from the top model selection row (Claude / ChatGPT / Grok / Gemini / Perplexity), the model is not running the full preset flow.
- Grok solo is returning generic/freeform text blobs like the attached example instead of the full structured preset response.
- Previously solo models worked correctly.
- Each solo model should run the full preset orchestration by itself.

Required solo-model behavior:
For ANY solo model selected from the top model row:
1. resolve preset / prompt intent
2. run the same preset/profile orchestration logic
3. run the same proprietary/custom data gathering pipeline already defined for that prompt
4. use the selected model’s native search/tool capabilities
5. reason over proprietary data + retrieved context
6. return the correct structured preset response format

Important product requirement:
Solo mode is NOT a lightweight text-only path.
Solo mode should still use:
- prompt orchestration
- preset resolution
- INTENT_PROFILE / display_type contract
- proprietary data gathering
- search/tooling for the selected model
- structured response generation

Specific Grok solo requirement:
When Grok is selected as the solo model, it should use:
- the XaiSentimentProvider path
- X search
- native web search where supported
- the full preset/proprietary data flow
- the full final structured preset response
It should not return a collaborator-style text block.
It should behave like a full final solo model.

Implementation tasks:
1. Audit the solo-model dispatch path for all direct model selections:
   - claude
   - gpt-4o / ChatGPT
   - grok
   - gemini
   - perplexity
2. Confirm whether the solo-model path still:
   - resolves preset_intent
   - loads INTENT_PROFILE
   - runs _gather_data_safe(query_info)
   - builds the full structured system prompt via _build_prompt
   - returns the preset JSON/display contract
3. Restore that end-to-end flow if any model path is bypassing it.

4. For Grok solo specifically:
   - ensure the final solo path uses XaiSentimentProvider, not a collaborator/raw shortcut
   - ensure it gets the full final system prompt from _build_prompt
   - ensure it uses full search capabilities (X search + native web search if your provider supports both in final mode)
   - ensure it returns the final structured preset response, not raw collaborator findings

5. Verify that collaborator-mode behavior remains distinct:
   - Grok collaborator in Caelyn mode = focused collaborator findings
   - Grok solo mode = full end-to-end final model behavior

6. Verify that all solo models now return the same type of structured preset response they used to return before the regression.

Important constraints:
- Do NOT remove the proprietary data pipeline
- Do NOT simplify preset outputs into generic text
- Do NOT break Customize mode
- Do NOT break Caelyn mode
- Do NOT do a large refactor
- Fix the regression in the smallest clean way possible

What I want from you before coding:
1. Audit the exact solo-model flow and identify where the regression happened
2. Audit the chart-symbol / tradingview symbol path and identify where that regression happened
3. Show the minimum-change plan and exact files to modify

What I want after coding:
1. exact files changed
2. exact cause of Bug 1
3. exact cause of Bug 2
4. how solo-model full-flow behavior was restored
5. how Grok solo now differs from Grok collaborator
6. confirmation that preset structured responses are restored for solo models
7. confirmation that correct TradingView symbols are restored for Trending Now / commodities cards

Also provide 3 proof traces after the fix:
- Trending Now in Caelyn mode
- Grok solo on a preset route
- One other solo model (Claude or Gemini) on a preset route

Each proof trace should show:
- preset_intent
- data pipeline used
- final model path used
- whether native search/tools were enabled
- whether structured preset output was returned