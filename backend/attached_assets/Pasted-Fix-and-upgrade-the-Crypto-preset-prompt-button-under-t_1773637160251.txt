Fix and upgrade the "Crypto" preset prompt button under the "Sectors" section on the terminal page so it returns the correct crypto-specific structured response using my actual crypto APIs.

You only have access to the BACKEND repo.

Important constraints:
- Do NOT redesign the terminal page.
- Do NOT touch frontend yet.
- Do NOT break the other sector preset buttons.
- Do NOT force crypto through the equity-sector pipeline.
- Keep the same general sector-response layout/presentation style as the other sector buttons, but make the data gathering and ranking crypto-native.
- Keep changes targeted and minimal where possible.
- Reuse existing crypto integrations already in the project wherever they exist.
- Only add new integration work where needed, specifically DeFiLlama.
- All listed API secrets already exist in backend where required. Some sources require no key.

## Problem
When I click the "Crypto" preset button under the "Sectors" section on the terminal page, I should get a detailed crypto overview response with specific crypto tickers/tokens/perps and strong crypto-native market intelligence.

Right now the crypto preset is not behaving the way I want.

I want the Crypto preset to:
- use the same overall page/response structure as the other sector preset buttons
- but use crypto-native data sources instead of the equity stack
- show specific crypto assets / perp tickers / setups
- give me a detailed crypto market overview, regime, flows, perp positioning, setups, catalysts, and what to watch

## Required crypto data sources to use
Use these actual crypto sources for the Crypto preset response:

### 1) Hyperliquid
Use for:
- core market data
- perps
- open interest
- price changes
- tickers
- volume
- funding rates
- liquidations
- OHLCV / candle context where useful

### 2) CoinMarketCap
Use for:
- current listings
- ticker data
- global crypto market statistics
- broad market context

### 3) CoinGecko
Use for:
- market data
- historical data
- token-level market context
- additional coverage where CMC is thin

### 4) Artemis
Use for:
- net flows
- inflows / outflows
- regime change
- chain / asset overview
- supported asset metrics
- stablecoin activity / supply / transfer context where relevant

### 5) altFINS
Use for:
- technical setups
- technical analysis
- TA signals on crypto assets

### 6) DeFiLlama (new integration if not already present)
Add/use DeFiLlama for:
- TVL
- protocol / chain flows
- fees / revenue
- DEX volumes
- stablecoin context
- yields where relevant
- token unlocks
- perp / bridge / ecosystem health context where useful

### 7) Polymarket
Use Polymarket if useful for:
- BTC predictions
- crypto-related prediction market probabilities
- event probabilities relevant to crypto response/catalysts
Only include this where it genuinely adds value.

## Important rule
For the Crypto preset:
- do NOT prioritize Finviz / Finnhub / EDGAR / FMP as the core data stack
- this response should be powered primarily by the crypto-native sources above

## What the Crypto preset response should do
Keep the same broad structured response style as the other sector preset buttons, but make the content crypto-specific.

I want the Crypto preset response to include crypto-native versions of things like:
- market/regime overview
- what's moving
- top signal from each scanner/source
- watchlist / top setups / top assets to watch
- catalysts
- perp/funding/OI/liquidation context
- flows / stablecoin / DeFi / chain-health context
- portfolio / tactical bias

Return specific assets/tickers/perp symbols where relevant.

Examples of the kinds of things that should surface:
- BTC, ETH, SOL, etc.
- specific perp markets
- alt setups with strong technicals
- chains with strong inflows / net flows
- assets with strong funding/OI/liquidation setups
- DeFi ecosystems / protocols with notable TVL/volume/revenue shifts
- Polymarket probabilities if they materially affect crypto positioning

## Required backend behavior
Audit the exact current preset flow for the Crypto button and identify:
1. what preset key the frontend sends for Crypto
2. what backend alias/path it currently resolves to
3. whether it is still using the wrong pipeline
4. whether an older crypto-tuned path already exists in the repo and simply needs to be restored
5. where the crypto-specific prompt/contract currently lives, if it exists
6. where the response structure should stay aligned with the sector preset layout while the datasource layer should diverge

## Required fix
Make the smallest safe backend fix so that:
- the Crypto preset uses a crypto-native data pipeline
- the Crypto preset response returns detailed crypto market intelligence again
- the response includes specific crypto tickers/tokens/perps
- the response uses the required crypto-native APIs listed above
- the response still fits the terminal’s structured sector-style layout

## Preferred implementation approach
- Reuse all existing crypto integrations already present in the project
- Add DeFiLlama integration only where needed
- Use Polymarket selectively when it adds meaningful predictive/catalyst context
- Keep the response schema stable enough that frontend should not need a redesign
- If there is already an old tuned crypto response path in the code, restore/reconnect it rather than inventing a brand-new generic system

## Deliverables
1. Identify the exact root cause of why the Crypto preset is not returning the correct crypto-specific response.
2. Implement the minimum backend fix.
3. Show exact files changed.
4. Explain the final routing path for the Crypto preset.
5. Explain which of the required crypto data sources are now being used and where.
6. Explain what new DeFiLlama integration work was added, if any.
7. Explain how Polymarket is incorporated, if used.
8. Explain how the crypto response structure stays aligned with the sector preset layout while using crypto-native data.
9. Confirm that other sector presets were not changed.

## Acceptance criteria
The task is done when:
- clicking the Crypto preset returns a detailed crypto overview response again
- the response includes specific crypto tickers / tokens / perp symbols
- the response is powered primarily by:
  - Hyperliquid
  - CoinMarketCap
  - CoinGecko
  - Artemis
  - altFINS
  - DeFiLlama
  - Polymarket where useful
- the response keeps the same general terminal sector-response structure
- the Crypto preset is no longer behaving like a generic equity-sector response
- other sector preset buttons are not broken

Before coding, tell me:
- the exact current Crypto preset key/path
- whether an old working crypto-tuned path still exists in the repo
- the exact file(s) where the crypto routing/data-source mismatch is happening
- which of the above crypto APIs are already wired vs which still need integration work