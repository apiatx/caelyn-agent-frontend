Fix the regression that broke the "Crypto" and "Commodities" preset prompt buttons under the "Sectors" prompt section.

You only have access to the BACKEND repo.

Important constraints:
- Do NOT redesign these features.
- Do NOT create new crypto/commodities prompt logic from scratch unless absolutely necessary.
- Do NOT overwrite the prior tuned behavior with a new generic response.
- These two preset buttons were already working correctly before the recent sector-preset changes.
- The goal is to restore the previous working behavior for Crypto and Commodities with the minimum necessary backend fix.
- Do NOT break the other sector preset buttons that were recently updated.

## Context
Under the "Sectors" preset prompt section, there are also two more preset buttons:
- Crypto
- Commodities

Both were working correctly before the recent changes to the other sector preset buttons.
The data sources for Crypto are already in the code and the response formatting/behavior was already tuned the way I wanted.
Commodities was also already tuned and working the way I wanted.

After the recent sector prompt changes, Crypto and Commodities are no longer working/displaying properly.

I do NOT want a new version invented.
I want the prior tuned behavior restored.

## Your task
Audit the current backend preset routing and response pipeline for:
- Crypto
- Commodities

Find exactly where the recent changes to the sector preset system caused these two prompt buttons to stop working correctly.

Then restore the old intended behavior for these two presets only, with the smallest safe change.

## Required audit steps
1. Find the exact current preset keys / aliases / routing path for:
   - Crypto
   - Commodities
2. Compare the current broken path to the previously working path in the code/history.
3. Identify whether the regression came from:
   - preset alias mapping
   - category mapping
   - thematic/sector routing
   - prompt contract injection
   - response parser / formatter mismatch
   - display_type / structured payload mismatch
4. Verify whether the old tuned prompt/contract for Crypto and Commodities is still present in the codebase.
5. If it is still present, restore routing so those existing tuned paths are used again.
6. If some old logic was partially bypassed, reconnect it instead of inventing a new system.

## What I want restored
### Crypto
- restore the prior tuned crypto preset behavior
- use the already-existing crypto data sources in the code
- restore the intended response formatting and content structure that was previously working
- do NOT replace it with a generic sector/thematic response

### Commodities
- restore the prior tuned commodities preset behavior
- restore the intended response formatting and content structure that was previously working
- do NOT replace it with a generic sector/thematic response

## Important rules
- Do NOT make frontend changes.
- Do NOT change the UI contract unless absolutely necessary.
- Do NOT broadly refactor preset routing.
- Only fix the specific regression that caused Crypto and Commodities to stop working after the other sector changes.
- Preserve the already-working tuned behavior as much as possible.
- If the prior tuned prompt/contract exists in the code, prefer reusing it exactly.

## Deliverables
1. Identify the exact root cause of why Crypto and Commodities stopped working correctly.
2. Implement the minimum backend fix.
3. Show the exact files changed.
4. Explain how Crypto now routes again.
5. Explain how Commodities now routes again.
6. Explain where the prior tuned logic was found and how it was restored.
7. Confirm that no unrelated sector prompt behavior was changed.

## Acceptance criteria
The task is done when:
- Crypto works again using its prior tuned behavior and existing crypto data sources
- Commodities works again using its prior tuned behavior
- neither is replaced by a generic thematic/sector fallback
- no frontend changes are required just to restore them
- the fix is minimal and targeted
- other sector preset buttons are not broken

Start by auditing the exact preset routing and contract path for Crypto and Commodities, compare it to the older working path, and restore the old tuned behavior with the smallest safe change.