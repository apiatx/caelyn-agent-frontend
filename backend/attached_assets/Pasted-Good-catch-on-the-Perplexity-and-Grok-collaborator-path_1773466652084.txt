Good catch on the Perplexity and Grok collaborator-path gap.

Now I want one more audit before any further changes:

1. Gemini / Google grounding
Please audit the exact collaborator and final-model path for Gemini.
Tell me:
- whether Gemini currently uses any specific grounding/search configuration
- whether it uses a configured provider object or generic _call_alt_model
- whether a hard domain filter exists today
- whether you think Gemini should remain broad for research/context, or whether there is a safe soft preferred-source approach
Do NOT add a strict Gemini domain filter unless you can justify it clearly.

My current preference:
- Perplexity should stay tightly source-filtered for financial news/catalysts
- Gemini should remain broader for research/context unless there is a compelling reason to constrain it

2. Collaborator output structure
I do NOT want collaborator models producing end-user JSON display payloads.
Only the final reasoning model (or a direct solo model selected by the user) should produce the final user-facing structured response.

Please confirm:
- collaborator outputs are NOT user-facing JSON payloads
- only the final model writes the end-user response structure

Also tell me whether collaborator outputs should remain:
- raw short targeted text
or
- lightweight internal structured objects for synthesis

I want your recommendation, but with minimal added complexity. My current instinct is:
- Phase 1: short targeted findings are fine
- Later phase: lightweight internal structure may improve synthesis

3. Full model/settings audit
Please audit the exact live configuration path now used for each model in BOTH collaborator mode and final/solo mode:

For each of these, show:
- code path used
- model variant used
- timeout used
- search/grounding tools used
- source/domain filters used
- recency settings used
- token limits used
- whether cached/provider-specific settings are preserved

Models to audit:
- Grok collaborator
- Grok final/solo
- Perplexity collaborator
- Perplexity final/solo
- Gemini collaborator
- Gemini final/solo
- Claude final
- ChatGPT final/solo, if applicable

4. Important:
Do not make more changes yet unless you find another real configuration mismatch like the one you already fixed for Grok/Perplexity collaborators.
I want the audit first.

What I want back:
- a clear table by model and mode
- any mismatches still remaining
- your recommendation on whether Gemini should stay broad or get any source constraints
- your recommendation on raw text vs lightweight internal structure for collaborator outputs