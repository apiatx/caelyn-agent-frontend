I want to make a Phase 0 backend alignment pass for a UX rename in my app. This is NOT the full routing/orchestration refactor yet. The purpose is to support a cleaner product concept and naming model while minimizing risk.

Product naming goal:
- "Caelyn" should represent the automatic smart house mode
- "Customize" should represent the manual advanced collaboration mode/settings

What I need:
Please inspect the backend and identify where the current naming, enums, request payloads, mode identifiers, preset names, or response metadata might still use older terminology in a way that could conflict with this UI rename.

Tasks:
1. Search the backend for references to concepts like:
   - caelyn
   - default
   - collaboration
   - preset
   - reasoning model
   - collaborators
   - custom collaboration
   - agent mode
   - orchestration mode
   - any mode selection fields used by the frontend

2. Determine whether the backend currently distinguishes between:
   - automatic/default system mode
   - manual/custom collaboration mode
   If it does, document the current names and structure.

3. Make only low-risk naming alignment changes if needed to support the frontend rename.
   Example:
   - if the frontend sends a mode or preset name that should now map to "Caelyn" or "Customize", make that mapping backward-compatible
   - preserve compatibility with any existing payload shapes if possible

4. Add or update any internal normalization/mapping layer needed so the backend can gracefully accept:
   - old frontend terms
   - new frontend terms
   without breaking behavior

5. Do NOT implement the full routing matrix yet.
   Do NOT do a major refactor yet.
   Do NOT rewrite the monolith yet.
   This is only a Phase 0 alignment pass.

6. If useful, create a small central mapping/normalization helper for mode names, preset labels, or collaboration mode identifiers, so the frontend rename does not create fragile string mismatches.

7. If the backend currently has a notion of a "default" preset inside custom collaboration settings, keep it working. If you rename anything internally, keep aliases/backward compatibility.

8. If responses returned to the frontend include display labels or mode metadata, update them carefully so the UI can reflect:
   - Caelyn = automatic smart mode
   - Customize = manual advanced mode

What I want from you:
1. Audit the backend files relevant to this naming/mode system
2. Make only the minimal safe changes needed for Phase 0
3. Tell me:
   - which files were changed
   - what strings / enums / payload mappings were updated
   - what remains unchanged for the later architecture refactor
4. Explicitly call out:
   - anything risky
   - any old naming still embedded deeply in the monolith
   - any places where full Phase 1 routing work should happen later

Implementation priorities:
- backward compatibility first
- minimum blast radius
- avoid changing business logic
- avoid changing provider/model execution logic
- avoid large file rewrites unless necessary

Important:
This is not the modular refactor yet. This is just a backend compatibility and naming alignment pass to support the frontend UX cleanup.